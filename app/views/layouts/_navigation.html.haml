#navigation
  - navigation_links.each do |link|
    .link{:class => controller.controller_name == link[:controller] ? 'current' : ''}
      = link_to(link[:text], link[:path])
#user-info.small
  %span.row= current_user.login.capitalize
  |
  %span.row= link_to('My info')
  |
  %span.row= link_to('Log out', logout_path)