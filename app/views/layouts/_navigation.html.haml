#navigation
  - navigation_links.each do |link|
    .link{:class => controller.controller_name == link[:controller] ? 'current' : ''}
      = link_to(link[:text], link[:path])
%ul#user-info.small
  %li
    = link_to(current_user.login.capitalize)
    |
  %li
    = link_to('Log out', logout_path)