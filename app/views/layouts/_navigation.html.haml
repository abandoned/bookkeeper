#tabs
  - navigation_links.each do |link|
    .tab{:class => controller.controller_name == link[:controller] ? 'current' : ''}
      = link_to(link[:text], link[:path])

%ul#actions.small
  = yield(:actions)
  %li
    = link_to('Profile')
  %li
    = link_to('Log out', logout_path)