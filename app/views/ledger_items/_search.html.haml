- form_tag(collection_path, :method => 'get', :id => 'search', :autocomplete => 'off') do
  = label(:query, 'Search here...')
  = text_field_tag(:query, params[:query], :class => 'title')
  = submit_tag('Search', :name => nil)