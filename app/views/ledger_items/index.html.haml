- title "Ledger Items", false

- unless @cart.ledger_items.empty?
  = render :partial => "cart"

%h1
  %span Ledger Items

= render :partial => "search"

- if collection.size > 0
  %table.data
    %thead
      %tr.small
        %th ID
        %th Account
        %th Sender
        %th Recipient
        %th Date
        %th Amount
        %th Description
    %tfoot
      %tr.smaller
        %td{:colspan => 6, :class => "currency underline"}
          - counter = 0
          - @totals.each do |k, v|
            - counter += 1
            - if counter != @totals.size
              = monetize(v, k) + "<br />"
            - else
              = monetize(v, k)
    %tbody
      = render(:partial => "ledger_item", :collection => collection)
  = will_paginate(collection, :inner_window => 2, :class => "small pagination")
- else
  %p.small No ledger items found
      
.footer
  = link_to "New Ledger Item", new_ledger_item_path(:account => params[:account])